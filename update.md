# PictureTransformer v1.0.8 更新内容

- 修复桌面和开始菜单快捷方式未显示指定 PictureTransformer 图标的问题。
- 选择父文件夹后，安装目录输入框会立即补全 `PictureTransformer` 并显示实际安装路径。
- 图形界面程序放在安装主目录；命令行工具及其依赖统一放入 `bin` 子目录。
- 安装前可选择是否将 `bin` 加入系统 PATH，默认勾选。
