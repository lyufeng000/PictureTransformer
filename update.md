# PictureTransformer v1.0.7 更新内容

- CLI 新增 `-o/--output`，可为单张图片指定完整输出文件路径。
- CLI 和图形界面新增覆盖同名文件选项；默认仍安全地自动添加编号。
- CLI 支持位置输入路径、长参数、更多帮助别名，以及更细的失败退出码。
- 自动清理路径首尾引号和 Unicode 隐藏方向字符，并支持 `~` 用户目录。
- 验证 HEIC/HEIF 转 PNG 时保留 ICC、EXIF、DPI 和自动方向处理。
