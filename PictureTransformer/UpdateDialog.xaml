<Window x:Class="PictureTransformer.UpdateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PictureTransformer 更新"
        Width="660" Height="560" MinWidth="560" MinHeight="460"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="#F6F8FC" FontFamily="Microsoft YaHei UI">
    <Window.Resources>
        <Style x:Key="UpdateButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="#604FF2" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="20,10" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="9" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5140DC" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.45" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="LaterButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#30364A" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#E0E2EA" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="20,10" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </Window.Resources>

    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="*" />
            <RowDefinition Height="14" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="52" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border Width="44" Height="44" Background="#604FF2" CornerRadius="12">
                <TextBlock Text="↑" Foreground="White" FontSize="22" FontWeight="Bold"
                           HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="发现新版本" Foreground="#1A2133" FontSize="20" FontWeight="Bold" />
                <TextBlock x:Name="VersionText" Margin="0,3,0,0"
                           Foreground="#7A8192" FontSize="12" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" Background="White" BorderBrush="#E0E2EA"
                BorderThickness="1" CornerRadius="12" Padding="18">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="10" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Text="更新内容" Foreground="#30364A" FontSize="14" FontWeight="SemiBold" />
                <TextBox x:Name="ReleaseNotesTextBox" Grid.Row="2"
                         IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                         BorderThickness="0" Background="Transparent" Foreground="#4B5161"
                         FontSize="12" Padding="0" />
            </Grid>
        </Border>

        <StackPanel Grid.Row="4">
            <ProgressBar x:Name="DownloadProgress" Height="6" Minimum="0" Maximum="100"
                         Foreground="#604FF2" Background="#E3E0FA" Visibility="Collapsed" />
            <TextBlock x:Name="StatusText" Margin="0,8,0,0" Foreground="#7A8192"
                       FontSize="11" Visibility="Collapsed" />
        </StackPanel>

        <Grid Grid.Row="6">
            <TextBlock Text="安装更新时软件会自动退出" Foreground="#8B91A0"
                       FontSize="10" VerticalAlignment="Center" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="LaterButton" Style="{StaticResource LaterButtonStyle}"
                        Content="稍后再说" Click="LaterButton_OnClick" />
                <Button x:Name="UpdateButton" Margin="10,0,0,0"
                        Style="{StaticResource UpdateButtonStyle}"
                        Content="立即更新" Click="UpdateButton_OnClick" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
